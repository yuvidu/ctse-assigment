server:
  port: ${SERVER_PORT:8085}

spring:
  application:
    name: payment-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb+srv://yuvidu:yuvidu@cluster1.kt7i4.mongodb.net/microservice?retryWrites=true&w=majority&appName=Cluster1}
  mail:
    host: ${SMTP_HOST:smtp.gmail.com}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USERNAME:vihanduk@gmail.com}
    password: ${SMTP_PASSWORD:jzjifebosldgmbbv}
    properties:
      mail:
        smtp:
          auth: ${SMTP_AUTH:true}
          starttls:
            enable: ${SMTP_STARTTLS:true}

management:
  endpoints:
    web:
      exposure:
        include: health,info

booking:
  service:
    base-url: ${BOOKING_SERVICE_BASE_URL:http://localhost:8082}

user:
  service:
    base-url: ${USER_SERVICE_BASE_URL:http://localhost:8081}
    # When true, payment-service loads email/name from user-service for logged-in checkout.
    enabled: ${USER_SERVICE_ENABLED:true}

payment:
  gateway:
    provider: ${PAYMENT_GATEWAY_PROVIDER:stripe-checkout}
    stripe:
      api-key: ${STRIPE_API_KEY:sk_test_51RgeLzGabNTWU4v4kFXBDMhxrtjcHZ1C1y9Vqlrjae4D7RoCPwGp826SVFUxZFLLjCme9xBOyzK37gLQivh0jPDt00wdhVrUXa}
      # ISO code (e.g. usd, lkr). Must be enabled for your Stripe account; `usd` avoids cross-currency minimum issues.
      checkout-currency: ${STRIPE_CHECKOUT_CURRENCY:usd}

# SmtpEmailService "From" header — defaults to SMTP_USERNAME when MAIL_FROM is unset
mail:
  from: ${MAIL_FROM:${SMTP_USERNAME:no-reply@motionpictures.local}}

